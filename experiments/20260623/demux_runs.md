# Demultiplexing runs — experiment 20260623

**None.** This experiment was aborted after the second preculture (media prepared wrong),
so nothing was PCR'd or sequenced. There is no raw read data and no demultiplexing to
point at.

The setup files in `setup/` are still useful — the design was reused as the starting point
for later experiments. See [analysis_index.md](analysis_index.md).
