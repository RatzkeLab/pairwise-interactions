# Known issues — experiment 20260623

---

## The experiment was aborted — NO DATA

Aborted after the second preculture: the media had been prepared wrong. Nothing was
amplified or sequenced.

Kept as a design reference only. Nothing in this folder should be treated as
measurements, and no result anywhere depends on it.

---

## The layout files here are a design ancestor, not a record of anything run

`setup/` contains a full strain layout, primer layout and Echo transfer set. They describe
a plate that was never successfully cultured. Reusing them as a template is fine; citing
them as what was physically done is not.
